# Base URL of the backend API, including the /api prefix.
# Vite inlines this at BUILD time (not read at container runtime) - see frontend/Dockerfile
# and docker-compose.yml's frontend build.args for how Docker Compose supplies it.
VITE_API_BASE_URL=http://localhost:8080/api
