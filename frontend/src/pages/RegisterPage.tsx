import { Container, Typography } from '@mui/material';

const RegisterPage = () => {
  return (
    <Container>
      <Typography variant="h4">Register Page</Typography>
      <Typography>Registration form implementation - similar to LoginPage</Typography>
    </Container>
  );
};

export default RegisterPage;
