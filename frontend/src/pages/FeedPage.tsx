import { Container, Typography } from '@mui/material';

const FeedPage = () => {
  return (
    <Container>
      <Typography variant="h4">Feed Page</Typography>
      <Typography>
        Personalized feed with infinite scroll implementation using React Query and
        Intersection Observer
      </Typography>
    </Container>
  );
};

export default FeedPage;
