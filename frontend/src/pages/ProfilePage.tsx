import { Container, Typography } from '@mui/material';

const ProfilePage = () => {
  return (
    <Container>
      <Typography variant="h4">Profile Page</Typography>
      <Typography>User profile with edit capabilities and posts</Typography>
    </Container>
  );
};

export default ProfilePage;
