import { Container, Typography } from '@mui/material';

const FollowersPage = () => {
  return (
    <Container>
      <Typography variant="h4">Followers Page</Typography>
      <Typography>Display followers and following lists with follow/unfollow actions</Typography>
    </Container>
  );
};

export default FollowersPage;
